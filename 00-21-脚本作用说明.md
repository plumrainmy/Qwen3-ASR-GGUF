# 00-21 脚本作用说明

本文档用于记录当前仓库中 `00*` 到 `21*` 这些脚本的主要作用和意义，方便快速理解整个 Qwen3-ASR / Aligner 的导出、转换、量化与运行链路。

当前仓库实际存在的编号是：

- `00-07`
- `11-18`
- `21`

当前未看到对应脚本的编号是：

- `08-10`
- `19-20`

## 整体流程概览

这些脚本大致分成两条链路：

- ASR 主识别链路：`00-07`、`21`
- Aligner 强制对齐链路：`11-18`

每条链路内部又基本遵循同样的阶段：

1. 导出前处理或编码器
2. 导出解码器
3. 转换为 GGUF
4. 量化
5. 最终运行推理

## 00-07：ASR 主识别链路

### 00-Export-Mel-Filters.py

主要作用：

- 从 `WhisperFeatureExtractor` 中提取 `mel_filters`
- 如果模型对象里没有现成的 `mel_filters`，就按 Whisper / Qwen3-ASR 的标准参数手动生成
- 将结果保存为 `mel_filters.npy`

意义：

- 固化语音前处理中的关键滤波器矩阵
- 保证不同运行环境下的 Mel 特征提取结果一致
- 方便后续 C++ / GGUF / ONNX 推理侧直接加载，而不必重新依赖 `transformers`

为什么 GGUF / C++ 推理侧尤其需要单独导出成 `.npy` 文件：

- GGUF 主要解决的是模型权重的存储和推理问题，但语音模型除了权重之外，还依赖完整的音频前处理链路。
- 在 Python 环境里，`transformers` 和 `WhisperFeatureExtractor` 会帮你自动生成或加载这些前处理参数；但到了 C++ / GGUF 推理侧，这些高级封装通常不存在。
- 如果不提前把 `mel_filters` 导出来，部署侧就必须自己重新实现 Mel 滤波器生成逻辑；而这一步涉及采样率、FFT bin 数、Mel 标度、频率上下限等多个细节，稍有不一致，最终特征就会偏掉。
- 一旦前处理和原模型训练时使用的规则不一致，哪怕模型权重本身是对的，识别结果也可能明显下降。
- 单独导出为 `.npy` 后，C++ / GGUF 侧不需要关心“这张滤波器表是怎么生成的”，只需要读取这个固定矩阵并参与后续计算即可。
- 这样可以把 Python 端“隐式存在于代码里的前处理规则”，变成一个“显式、可携带、可复用的数值文件”。

它带来的实际价值主要有：

- 避免部署侧重复实现并降低实现偏差风险
- 保证 Python、ONNX、GGUF、C++ 多个环境之间的前处理一致性
- 降低部署端对 `transformers` 的依赖
- 方便调试时做数值对齐，快速判断问题是在前处理还是在模型推理
- 简化跨语言移植流程，让推理侧只负责“使用矩阵”，不负责“生成矩阵”

一句话理解：

- `mel_filters.npy` 不是模型权重，而是语音前处理中的一张固定变换表。
- 对 GGUF / C++ 推理来说，把它提前导出，就是把“模型能否正确听懂音频”的关键前处理参数一起打包带走。

### 01-Export-ASR-Encoder-Frontend.py

主要作用：

- 加载 ASR 模型
- 从 `audio_tower` 中拆出 Encoder 的前端部分
- 导出为静态形状的 ONNX 文件 `qwen3_asr_encoder_frontend.fp32.onnx`

意义：

- 将音频编码器拆成更小、更稳定的前端模块
- 这个前端主要负责对固定大小的 Mel Chunk 做局部特征提取
- 拆分后更有利于部署、调试、以及不同推理后端的适配

### 02-Export_ASR-Encoder-Backend.py

主要作用：

- 加载 ASR 模型中的 `audio_tower`
- 导出编码器后端 Transformer 部分为 ONNX
- 支持动态时间长度输入

意义：

- 负责处理前端输出的隐藏特征序列
- 这部分通常是长序列建模的核心
- 与前端拆开后，可以单独优化、量化和调试序列建模部分

### 03-Optimize-ASR-Encoder.py

主要作用：

- 对 ASR Encoder 的 frontend / backend ONNX 文件执行 ONNX Runtime 图优化
- 输出优化后的模型，并打印 Opset 与算子分布诊断信息

意义：

- 通过算子融合减少图复杂度
- 提高推理执行效率
- 让导出的 ONNX 更适合后续量化与部署

### 04-Quantize-ASR-Encoder.py

主要作用：

- 将 ASR Encoder 的 FP32 ONNX 模型批量转换为：
- `FP16`
- `INT8`
- `INT4`

意义：

- 提供不同精度版本，便于在速度、显存、精度之间做权衡
- `FP16` 适合 GPU / DML 场景
- `INT8` / `INT4` 更适合进一步压缩体积和降低资源占用

### 05-Export-ASR-Decoder-HF.py

主要作用：

- 从完整的 Qwen3-ASR 模型中提取解码器，也就是 `thinker` 里的文本生成部分
- 重组配置与权重
- 保存为一个独立的 Hugging Face 风格目录 `asr_decoder_hf`

意义：

- 把原始 ASR 模型里的 LLM 解码器单独剥离出来
- 为后续 GGUF 转换准备标准 HF 格式输入
- 这是将“语音模型中的文本解码器”接入 `llama.cpp / GGUF` 链路的重要中间步骤

### 06-Convert-ASR-Decoder-GGUF.py

主要作用：

- 读取 `05` 导出的 HF 解码器目录
- 通过 `convert_hf_to_gguf` 转为 GGUF
- 对转换器打猴子补丁，绕开配置识别和词表判断问题

意义：

- 让 ASR 的文本解码器进入 GGUF 生态
- 解决该模型并非原生标准文本模型时的兼容问题
- 这是 ASR 解码器可以在 `llama.cpp` 风格推理框架中运行的关键转换步骤

### 07-Quantize-ASR-Decoder-GGUF.py

主要作用：

- 调用 `llama-quantize.exe`
- 将 `qwen3_asr_llm.f16.gguf` 量化为 `q4_k` 版本

意义：

- 进一步压缩 ASR 解码器 GGUF 文件体积
- 降低运行时内存占用
- 提升普通设备上的可部署性

## 11-18：Aligner 强制对齐链路

### 11-Export-Aligner-Encoder-Frontend.py

主要作用：

- 加载 Aligner 模型
- 导出其音频编码器前端为 ONNX
- 产物为 `qwen3_aligner_encoder_frontend.fp32.onnx`

意义：

- 与 ASR 链路中的前端职责类似
- 负责对固定大小的音频块做基础特征提取
- 为后续长序列对齐计算提供稳定输入

### 12-Export-Aligner-Encoder-Backend.py

主要作用：

- 导出 Aligner 的音频编码器后端 Transformer 为 ONNX
- 支持动态长度序列和 4D attention mask

意义：

- 这是对齐器处理长时间音频特征序列的核心部分
- 与前端拆分后，更便于处理对齐任务中常见的长上下文输入
- 单独导出后，也更容易针对对齐任务做专项优化

### 13-Optimize-Aligner-Encoder.py

主要作用：

- 对 Aligner Encoder 的 frontend / backend ONNX 模型做图优化
- 同时输出算子与 Opset 诊断信息

意义：

- 与 `03` 的作用对应
- 提高 Aligner 编码器推理效率
- 为后续精度转换和部署做好准备

### 14-Quantize-Aligner-Encoder.py

主要作用：

- 将 Aligner Encoder 的 FP32 ONNX 模型批量转换为：
- `FP16`
- `INT8`
- `INT4`

意义：

- 为对齐器提供多种部署精度版本
- 在精度允许范围内尽量减少体积与显存占用
- 方便根据设备能力切换模型版本

### 15-Export-Aligner-Decoder-HF.py

主要作用：

- 从 Qwen3ForcedAligner 中提取 `thinker` 文本解码器部分
- 生成独立 HF 目录 `aligner_decoder_hf`
- 对 `lm_head.weight` 进行补齐，使其适配目标词表大小

意义：

- 对齐器的解码器结构不是直接可用于标准 HF / GGUF 转换的，需要额外整理
- 通过权重补齐与配置伪装，使其能够进入后续 GGUF 转换链路
- 这是对齐器 LLM 独立部署的核心预处理步骤

### 16-Convert-Aligner-Decoder-GGUF.py

主要作用：

- 读取 `15` 导出的 HF 目录
- 调用 `convert_hf_to_gguf` 转成 `qwen3_aligner_llm.f16.gguf`
- 同样通过猴子补丁解决配置识别和分词器识别问题

意义：

- 让 Aligner 的文本解码器也进入 GGUF 推理体系
- 与 ASR 解码器链路保持一致，统一部署方式
- 降低自定义模型结构带来的转换阻力

### 17-Quantize-Aligner-Decoder-GGUF.py

主要作用：

- 调用 `llama-quantize.exe`
- 把 `qwen3_aligner_llm.f16.gguf` 量化为 `q4_k`

意义：

- 降低对齐器解码器的资源消耗
- 提高在本地环境中的运行可行性
- 让对齐功能可以在更受限的硬件上部署

### 18-Run-Aligner.py

主要作用：

- 初始化 `QwenForcedAligner`
- 加载音频和文本
- 执行强制对齐，输出每个文本片段的开始/结束时间

意义：

- 这是整个 Aligner 链路的最终验证脚本
- 用来确认导出的模型、量化模型和推理逻辑能否真正协同工作
- 最终产物是带时间戳的对齐结果，可用于字幕、切片、校对等场景

## 21：ASR 最终运行脚本

### 21-Run-ASR.py

主要作用：

- 初始化 `QwenASREngine`
- 加载音频并执行语音识别
- 可选启用 Aligner 获得时间戳
- 导出识别结果为 `txt`、`srt`、`json`

意义：

- 这是整个 ASR 导出与转换链路的最终落地入口
- 它把前面生成的编码器、解码器、GGUF、对齐器能力全部串起来
- 最终面向的是实际业务使用，而不是中间格式转换

## 文件之间的依赖关系

可以把这些脚本理解为两条装配线。

ASR 链路：

1. `00` 导出 Mel 滤波器
2. `01`、`02` 导出 Encoder 前后端 ONNX
3. `03` 优化 Encoder ONNX
4. `04` 量化 Encoder
5. `05` 导出 Decoder 为 HF 格式
6. `06` 转换 Decoder 为 GGUF
7. `07` 量化 Decoder GGUF
8. `21` 运行完整 ASR 推理

Aligner 链路：

1. `11`、`12` 导出 Aligner Encoder 前后端 ONNX
2. `13` 优化 Aligner Encoder ONNX
3. `14` 量化 Aligner Encoder
4. `15` 导出 Aligner Decoder 为 HF 格式
5. `16` 转换 Aligner Decoder 为 GGUF
6. `17` 量化 Aligner Decoder GGUF
7. `18` 运行强制对齐

## 一句话总结

如果只看本质：

- `00` 负责准备语音前处理参数
- `01-04` 和 `11-14` 负责把音频编码器拆分、导出、优化、量化
- `05-07` 和 `15-17` 负责把文本解码器提取、转换成 GGUF、再量化
- `18` 和 `21` 负责最终运行验证与实际推理输出
